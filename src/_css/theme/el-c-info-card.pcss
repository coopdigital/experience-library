 
.el-c-info-card {
    margin: var(--spacing-16) 0 var(--spacing-32) 0;
    padding: var(--spacing-16) var(--spacing-32);
    background: var(--color-white);
    border-radius: var(--ui-border-radius);
    box-shadow: var(--ui-shadow);
}

.el-c-info-card__image {
    width: 100%;
}